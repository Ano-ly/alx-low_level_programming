#include <stdio.h>

/**
 * main - entry point
 * Description - the program starts here
 * Return: always 0
*/

int main(void)
{
	char sent[60] = "Programming is like building a multilingual puzzle";

	puts(sent);
	return (0);
}
