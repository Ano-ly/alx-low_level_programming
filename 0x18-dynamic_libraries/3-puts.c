#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _puts - prints a string, followed by a new line
 * @str: string to be printed
 * Description - prints a string
 * Return: void
*/

void _puts(char *str)
{
	printf("%s\n", str);
}
